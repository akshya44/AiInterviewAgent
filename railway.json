{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install --prefix server && npm install --prefix client && npx prisma generate --schema=server/prisma/schema.prisma && npm run build --prefix client"
    },
    "deploy": {
        "startCommand": "npm start --prefix server",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}